{
  "name": "solid_santo",
  "version": "1.0.0",
  "description": "O acrônimo SOLID representa os cinco princípios que facilitam o processo de desenvolvimento — o que facilita a manutenção e a expansão do software.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start-d-problem": "ts-node-dev -r tsconfig-paths/register  --ignore-watch node_modules src/dependency_inversion/problem/index.ts",
    "start-d-solution": "ts-node-dev -r tsconfig-paths/register  --ignore-watch node_modules src/dependency_inversion/solution/index.ts"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.4.5"
  }
}
